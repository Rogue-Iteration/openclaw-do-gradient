# ═══════════════════════════════════════════════════════════════════
# OpenClaw + Gradient AI — Environment Configuration
# ═══════════════════════════════════════════════════════════════════
#
# HOW TO USE:
#   1. Copy this file:       cp .env.example .env
#   2. Fill in your values below
#   3. Deploy:               bash install.sh
#
# See README.md for step-by-step instructions on where to get
# each key.
#
# ═══════════════════════════════════════════════════════════════════


# ─── Gradient AI (required) ──────────────────────────────────────
# Powers the LLM inference for all research agents.
#
# Where to get it:
#   1. Log into the DigitalOcean Console
#   2. Go to Agent Platform
#   3. Click "Create model access key"
#
GRADIENT_API_KEY=


# ─── Telegram Bots (required) ────────────────────────────────────
# Each research team member runs as a separate Telegram bot.
# Create 4 bots via @BotFather (/newbot) and paste tokens below.
#
# In a Telegram group, @mention a bot to talk to that agent.
# Unmentioned messages go to Max (the team lead / default).
#
MAX_TELEGRAM_BOT_TOKEN=
NOVA_TELEGRAM_BOT_TOKEN=
LUNA_TELEGRAM_BOT_TOKEN=
ACE_TELEGRAM_BOT_TOKEN=


# ─── DigitalOcean API (required for install.sh) ──────────────────
# Used by install.sh to create and manage your Droplet.
# Not needed if you deploy manually (e.g., docker compose on
# your own server).
#
# Where to get it:
#   1. DigitalOcean Console → API
#   2. Click "Generate New Token"
#   3. Scope: Full Access (or minimum: Droplet, GenAI, Spaces)
#
DO_API_TOKEN=


# ─── DigitalOcean Spaces (required) ─────────────────────────────
# Object storage for research data. Your research reports are
# stored here and indexed by the Knowledge Base for RAG queries.
#
# Setup steps:
#   1. DO Console → Spaces Object Storage → Create Bucket
#   2. Pick a region and name (e.g., "openclawresearch")
#   3. Select the Access Keys tab → create a key with full access
#   4. Fill in the values below
#
DO_SPACES_ACCESS_KEY=
DO_SPACES_SECRET_KEY=
DO_SPACES_ENDPOINT=https://nyc3.digitaloceanspaces.com
DO_SPACES_BUCKET=openclawresearch


# ─── Gradient Knowledge Base (required) ──────────────────────────
# RAG-powered Q&A over your stored research. The Knowledge Base
# indexes documents from your Spaces bucket so agents can answer
# questions like "What do you know about $CAKE?"
#
# Setup steps:
#   1. DO Console → Agent Platform → Knowledge Bases tab
#   2. Click "Create Knowledge Base"
#   3. Select your Spaces bucket as the data source
#   4. Copy the UUID from the Knowledge Base detail page
#
GRADIENT_KB_UUID=
